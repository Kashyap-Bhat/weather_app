import { GetResourceResponse, RequestParameters } from 'maplibre-gl';
import { OmProtocolSettings, TileJSON } from './types';
export declare const defaultOmProtocolSettings: OmProtocolSettings;
export declare const omProtocol: (params: RequestParameters, abortController: AbortController, settings?: OmProtocolSettings) => Promise<GetResourceResponse<TileJSON | ImageBitmap | ArrayBuffer | null>>;
export declare const normalizeUrl: (url: string) => Promise<string>;
