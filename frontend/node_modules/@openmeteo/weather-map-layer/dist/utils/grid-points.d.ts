import { GridInterface } from '../grids';
import { default as Pbf } from 'pbf';
import { ResolvedClippingOptions } from './clipping';
/**
 * Generate the PBF grid-point layer for a single tile.
 * Computes tile geographic bounds that intersects with `clippingBounds` if defined
 */
export declare const generateGridPoints: (pbf: Pbf, grid: GridInterface, values: Float32Array, directions: Float32Array | undefined, x: number, y: number, z: number, clippingOptions?: ResolvedClippingOptions, extent?: number, margin?: number) => void;
