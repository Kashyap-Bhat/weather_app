import { GridInterface } from '../grids';
import { default as Pbf } from 'pbf';
import { ResolvedClippingOptions } from './clipping';
export declare const generateArrows: (pbf: Pbf, values: Float32Array, directions: Float32Array, grid: GridInterface, x: number, y: number, z: number, clippingOptions: ResolvedClippingOptions | undefined, extent?: number, arrows?: number) => void;
